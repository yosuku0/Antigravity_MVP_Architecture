job_id: JOB-20250425-PLAN-000
type: review
domain: game
squads: [review_squad, coding_squad]
objective: |
  【役割】
  あなたは Antigravity MVP の Agent Manager（Plan Mode）です。
  `work/jobs/` に配置された以下の 3 つの JOB ファイルを精読し、実装計画（Implementation Plan）を立案してください。
  各 JOB を **1つずつ順序立てて** 実行するための詳細計画を出力し、私（人間オペレーター）の承認を得てから Execute Mode に移行できるように準備してください。

  【配置済み JOB ファイル】
  1. `work/jobs/JOB-20250425-HITL-001.md` — HITL 承認フロー実装（approve.py Gate 関数補完）
  2. `work/jobs/JOB-20250425-AUDIT-002.md` — audit・ジョブ状態の終端状態再定義と daemon 安全パッチ
  3. `work/jobs/JOB-20250425-SEC-003.md` — Domain fail-closed 化 + ブラウザリーク修復 + subprocess 自己矛盾解消

  【プロジェクト前提（不変条件）】
  - Python 3.12 必須（CrewAI が 3.14 と非互換）
  - すべてのファイル書き込みは `utils.atomic_io.atomic_write()` / `atomic_append()` を使用
  - `os.O_CREAT | os.O_EXCL` によるアトミックロック
  - Domain 分離: `derive()` のみがクロスドメインの唯一手段
  - テストは `pytest tests/ -v` で実行、各タスク完了後に `git commit`
  - MVP 1.1.0 Phase B 完了状態（domains/ + knowledge_os.py + 17テスト全通過）
  - `graph.py` ノード構成: load_job → router → squad_router → execute_squads → audit → END（ノード追加/削除禁止、今回は既存ノードの return 分岐と内部ロジックのみ変更）

  【計画立案の要件】
  1. タスク順序と依存関係の分析:
     - 3つの JOB のうち、どれから始めるべきかを論理的依存関係とリスクで説明。
     - 各タスクが完了したら git commit し、次のタスクに進む体制を明示。

  2. ファイル変更マッピング:
     - 各 JOB に対して「変更するファイル」「変更しないが影響を受けるファイル」「新規作成ファイル」を表形式で出力。
     - 各ファイルの「変更箇所（関数/クラス名）」を特定。

  3. 段階的実装プラン（Step-by-Step Breakdown）:
     - 各 JOB を 3〜5 の小ステップに分解。
     - 各ステップの「入力」「処理」「出力（動作確認ポイント）」を記述。
     - 1つのステップは 1 ファイルの 1 関数に集中する粒度。

  4. テスト戦略:
     - 各 JOB に対して「既存テストの更新」「新規テストの追加」「手動確認項目」を明記。
     - `pytest tests/ -v` の実行タイミング（各ステップ後、または JOB 完了後）を指示。

  5. リスクとフォールバック:
     - 各 JOB の最大リスク箇所を特定し、「計画通りに進まなかった場合のロールバック手順」を記述。
     - 特に `graph.py` と `wiki_daemon.py` は稼働中の制御面の根幹であるため、変更前の状態に即復元できるよう `.bak` 不要（git で管理）であることを前提にした復元手順を示す。

  6. 差分最小化の確認:
     - 各変更について「これ以上小さくできない理由」を説明。
     - 例: 「approve.py に frontmatter 読み書きヘルパーを追加するのは、3つの Gate 関数が共通で必要なため。ヘルパーを共通化しないとコード重複が生じる」

  【期待する出力フォーマット】
  以下のセクション構成で計画書を出力してください。余計な説明は省き、計画のみを出力。

  --- 出力フォーマット ---
  # Antigravity 修復計画書（Plan Mode Output）

  ## 1. タスク順序と依存関係
  （タスク順序図と、なぜその順序かの論理的根拠）

  ## 2. ファイル変更マッピング
  | JOB | 変更ファイル | 変更箇所 | 影響ファイル | 新規ファイル |
  |-----|-----------|---------|------------|------------|

  ## 3. 段階的実装プラン
  ### JOB-1: HITL 承認フロー
  - Step 1: ...
  - Step 2: ...
  ### JOB-2: Audit・Daemon 安全パッチ
  - Step 1: ...
  ### JOB-3: Domain 安全 + リーク修復
  - Step 1: ...

  ## 4. テスト戦略
  （各 JOB のテスト計画）

  ## 5. リスクとフォールバック
  （最大リスクとロールバック手順）

  ## 6. ネクストアクション
  「以上の計画で問題なければ、JOB-1 の Step 1 から Execute Mode で実行を開始します。」
  --- 出力フォーマットここまで ---

  【厳守事項】
  - この Plan Mode ではコードを書かない。計画のみを出力。
  - 既存のアーキテクチャ（LangGraph ノード構成、KnowledgeOS インターフェース、atomic_io パターン）を変更しないことを前提に計画する。
  - 推測での破壊的変更（ステートマシンの追加ノード、ファイル構成の変更等）は計画に含めない。最小修正のみ。
priority: P0