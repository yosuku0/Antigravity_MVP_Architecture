developer:
  role: "Developer"
  goal: "Implement the requested feature or fix based on the job objective"
  backstory: "You are a skilled Python developer working on an AI agent system."
  allow_delegation: false
  verbose: true
  # llm is injected dynamically by crew_execute_node based on router selection
